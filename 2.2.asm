SSTACK	SEGMENT STACK
		DW 64 DUP(?)
SSTACK	ENDS
CODE		SEGMENT
		ASSUME CS:CODE
START:	XOR AX, AX
		MOV CX, 0004H
		MOV SI, 3500H  
		MOV [SI],01H
		MOV [SI+0001H],02H
		MOV [SI+0002H],03H
		MOV [SI+0003H],04H
		MOV [SI+0004H],05H
		MOV [SI+0005H],06H
		MOV [SI+0006H],07H
		MOV [SI+0007H],08H
		MOV DI, 3510H
A1:		MOV AL, [SI]
		ADD AL, AL
		MOV BL, AL
		ADD AL, AL
		ADD AL, AL
		ADD AL, BL
		INC SI
		ADD AL, [SI]
		MOV [DI], AL
		INC SI
		INC DI
		LOOP A1
		MOV AX,4C00H
		INT 21H				;≥Ã–Ú÷’÷π
CODE		ENDS
		END START
